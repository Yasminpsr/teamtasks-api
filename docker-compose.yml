
services:
  postgres:
    image: postgres:15
    container_name: teamtasks-postgres
    environment:
      POSTGRES_DB: teamtasks
      POSTGRES_USER: teamtasks
      POSTGRES_PASSWORD: teamtasks
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: